# Transfer UI (Vue 3 + Vite)

## 🚀 Como rodar

```bash
cd transfer-ui
npm install
npm run dev
```

O app rodará em [http://localhost:5173](http://localhost:5173)

Certifique-se que o backend Spring Boot está ativo em `http://localhost:8080`.
